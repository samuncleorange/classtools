services:
  app:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      # 容器时区,影响家长端"今日得分"的日界(默认上海)
      TZ: ${TZ:-Asia/Shanghai}
    ports:
      - "${HOST_PORT:-8080}:8080"
    volumes:
      - ./data:/data
