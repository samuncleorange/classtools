# 服务端口（容器内）
PORT=8080
# 宿主映射端口（docker compose 用，默认 8080）
# HOST_PORT=8080
# 数据目录（SQLite 与上传文件）。请勿删除此行：省略时回退到容器内 ./data，不会持久化。
DATA_DIR=/data
# 会话签名密钥（请改成随机长字符串，例如 openssl rand -hex 32）
SESSION_SECRET=change-me-to-a-long-random-string
# 初始管理员账号（仅在数据库为空时创建一次）
ADMIN_USERNAME=teacher
ADMIN_PASSWORD=change-me
# production = 启用 Secure Cookie（需通过 HTTPS 访问，适合反向代理部署）；本机用 http 直连测试时改为 development
NODE_ENV=production
