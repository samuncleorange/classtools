# Dependencies
node_modules/

# Build output
dist/
build/

# Runtime data (SQLite db + uploads) — never commit
data/

# Env / secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
*.log

# Original vendor tutorial (local reference only — contains vendor's copyrighted images)
班级宠物养成系统使用教程/
